version: 2
jobs:
  build:
    machine: true
    working_directory: ~/openJDK-glcloud
    steps:
       # Get the code    
      - checkout
      - run:
          command: |
            docker build -t my-cloud-sdk-docker:default .